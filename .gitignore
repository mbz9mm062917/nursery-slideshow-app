# OS
.DS_Store
Thumbs.db
ehthumbs.db
desktop.ini

# IDE
.idea/
.vscode/*
!.vscode/extensions.json
*.iml
*.suo
*.sw?

# Node (念のためルート直下も対象に。実体は frontend/node_modules)
**/node_modules/

# ビルド成果物(念のためのセーフティネット。実体は各サブプロジェクトの.gitignoreで管理)
**/dist/
**/dist-ssr/
**/build/
**/.gradle/

# ログ
*.log
**/logs/

# 環境変数のローカル上書き(secretsが混入しやすいため)。
# .env 自体はシークレットを含まない開発用デフォルト値なので追跡対象とする。
.env.local
.env.*.local
